踩坑及建议：
1、要熟悉比赛规则以及agent接口输入输出规则，比如返回的response字段，什么时候用JSON，什么时候是自然语言，我刚开始就踩坑了，刚开始是只要用户咨询关于房源的问题，都按json返回，实际上是只有在调用房源相关接口的时候，才按json返回(即使未查询到房源信息)
2、模型接口会偶现超时或失败的情况，要加重试机制
3、房源接口要注意入参，特别是租房接口，接口是POST类型，但调用的时候没有请求体，listing_platform字段是query参数，这个问题我踩过坑，群里不少人也遇到过
4、房源信息最多返回5个，有明确排序条件，如离地铁近、便宜等，按条件排序，如果无排序条件，则默认返回
5、其它场景
咨询两个房源信息，houses里面需要这两个房源ID；如果是问哪个离地铁近什么的，那就只保留符合条件的那一个
操作类问题(需要是明确指令，如果是xxx房子可以租吗？我想租，这个只查询，不操作)，如租房、退租、下架等，按JSON返回，houses里面是操作的房源ID